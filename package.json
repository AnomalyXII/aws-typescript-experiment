{
  "name": "@anomaly-xii/aws-lambda-experiment",
  "description": "A test of running TypeScript code in AWS Lambdas",
  "private": true,
  "author": "AnomalyXII",
  "license": "MIT",
  "version": "0.1.0",
  "main": "lib/index.js",
  "packageManager": "yarn@3.0.2",
  "devDependencies": {
    "typescript": "4.3.5"
  },
  "scripts": {
    "build": "yarn build:compile && yarn build:test",
    "build:compile": "yarn workspaces foreach run compile",
    "build:test": "yarn workspaces foreach run test",
    "clean": "yarn workspaces foreach run clean"
  },
  "workspaces": [
    "packages/**"
  ]
}
